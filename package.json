{
  "name": "writing-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "start": "npm --prefix web run start",
    "lint": "npm --prefix web run lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "WebTest (web-only): Next.js UI + Python Playwright page analyzer"
}
